## BaseModel

also note that i do not want "bert-specific", i want it to be "general", can apply to various type of models.

continue generate the code, start from method "" ()
Check all again if you missed any detail. If so, write and return all of the source code for me.
Check all again if you missed any detail. And return all of the source code for me. 

Also consider:

- Using Ray Train for training (Adding a toggle variable to indicate whether we want to use Ray Train.)

- in ModelTrainer, why i do not see the input of callback, just see it init with empty list

---

- QuantizationManager
  - input: model (nn.Module)

